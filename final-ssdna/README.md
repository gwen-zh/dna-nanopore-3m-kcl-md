# Final formal ssDNA data

Each `final-50ns` directory contains the final formal system, NAMD inputs,
stage logs, the latest checkpoint from every stage, final 50 ns analyses, and
the representative structure selected from the last 10 ns.

Files ending in `.old` and duplicate final-coordinate outputs were omitted.
The DCD trajectories are described by the repository-level data manifest.
